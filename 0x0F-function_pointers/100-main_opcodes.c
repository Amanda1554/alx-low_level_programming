#include <stdlib.h>

/**
  * main - prints opcodes of its main function
  * @argc: counts the number of arguments
  * @argv: array vector
  * Return: hexadecimal opcodes
  */

int main(int argc, char *argv[])
{
	return (0);
}
