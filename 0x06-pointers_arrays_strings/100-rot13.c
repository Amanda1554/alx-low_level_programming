#include "main.h"

/**
  * rot13 - encodes a string using rot13
  * @s: pointer to a string
  * Return: returns s
  */

char *rot13(char *s)
{
	char *p;

	printf("%s", p);
	printf("------------------------------------\n");
	printf("%s", s);
	printf("------------------------------------\n");
	printf("%s", s);
	return (0);
}
